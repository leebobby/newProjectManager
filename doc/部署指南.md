# 岳麓山项目管理系统 · 部署指南

> 适用版本：v0.9.0+  
> 目标场景：企业内网单机部署，约 60 人团队并发访问

---

## 目录

1. [架构概述](#1-架构概述)
2. [服务器选型与环境要求](#2-服务器选型与环境要求)
3. [首次部署](#3-首次部署)
4. [数据备份与恢复](#4-数据备份与恢复)
5. [升级流程](#5-升级流程)
6. [常见问题排查](#6-常见问题排查)

---

## 1. 架构概述

```
                     ┌─────────────────────────────┐
  浏览器 60人  ──────▶│         Nginx               │
                     │  • 静态文件 (Vue 前端)        │
                     │  • 反向代理 /api → 后端       │
                     └────────────┬────────────────┘
                                  │
                     ┌────────────▼────────────────┐
                     │   Uvicorn + FastAPI 后端     │
                     │   监听 127.0.0.1:8000        │
                     └────────────┬────────────────┘
                                  │
                     ┌────────────▼────────────────┐
                     │   SQLite  (app.db)           │
                     │   config.json               │
                     └─────────────────────────────┘
```

**技术栈**
- 后端：Python 3.11 + FastAPI + Uvicorn
- 前端：Vue 3 + Vite（构建为静态文件，由 Nginx 直接服务）
- 数据库：SQLite（文件型，零维护）
- 反向代理：Nginx

---

## 2. 服务器选型与环境要求

### 2.1 硬件推荐

| 项目 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU  | 2 核     | 4 核     |
| 内存 | 2 GB     | 4 GB     |
| 磁盘 | 20 GB HDD | 50 GB SSD |
| 网络 | 内网百兆 | 内网千兆 |

> 60 人团队的项目管理工具以读操作为主，SQLite 完全胜任。上述配置有充足余量。

### 2.2 操作系统

推荐 **Ubuntu 22.04 LTS**（长期支持，5 年安全更新）。CentOS Stream 9 亦可，命令略有差异。

### 2.3 软件依赖

| 软件 | 版本要求 | 用途 |
|------|----------|------|
| Python | 3.11+ | 运行后端 |
| pip / venv | 随 Python | 依赖管理 |
| Node.js | 18 LTS+ | 构建前端 |
| npm | 9+ | 前端包管理 |
| Nginx | 1.18+ | 反向代理 + 静态文件 |
| Git | 任意 | 代码拉取与版本管理 |

---

## 3. 首次部署

> 以下所有命令在 **服务器** 上以 `ubuntu`（或其他普通用户）执行，需要 root 权限的步骤会显式使用 `sudo`。

### 3.1 安装系统依赖

```bash
sudo apt update && sudo apt upgrade -y

# Python 3.11
sudo apt install -y python3.11 python3.11-venv python3-pip

# Node.js 18 LTS（通过 NodeSource）
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# Nginx & 工具
sudo apt install -y nginx git sqlite3
```

验证安装：

```bash
python3.11 --version   # Python 3.11.x
node -v                 # v18.x.x
nginx -v                # nginx/1.x.x
```

### 3.2 拉取代码

```bash
# 选择一个合适的目录，例如 /opt
sudo mkdir -p /opt/aipm
sudo chown $USER:$USER /opt/aipm

cd /opt/aipm
git clone <你的仓库地址> app
# 或者直接上传压缩包后解压：
# unzip aipm.zip -d app
```

目录结构应如下：

```
/opt/aipm/app/
├── backend/          ← Python 后端
│   ├── main.py
│   ├── requirements.txt
│   ├── config.json   ← 会在首次启动后自动创建或手动编辑
│   └── app.db        ← 数据库（首次启动后自动创建）
└── frontend/         ← Vue 前端源码
    ├── src/
    └── package.json
```

### 3.3 部署后端

```bash
cd /opt/aipm/app/backend

# 创建虚拟环境
python3.11 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 编辑配置文件（关键：修改 issue_report_path 为服务器上的报表目录）
cp config.json.example config.json  # 如有示例文件
# 或者直接编辑：
nano config.json
```

`config.json` 示例：

```json
{
  "current_stages": ["BFI", "岳麓山", "明场"],
  "issue_report_path": "/opt/aipm/reports",
  "about_content": "岳麓山项目管理系统",
  "hero_badges": ["项目名称", "团队名称", "版本", "阶段"]
}
```

**创建 systemd 服务**（让后端开机自启并自动重启）：

```bash
sudo nano /etc/systemd/system/aipm-backend.service
```

填入以下内容：

```ini
[Unit]
Description=岳麓山项目管理系统后端
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/opt/aipm/app/backend
Environment="PATH=/opt/aipm/app/backend/venv/bin"
ExecStart=/opt/aipm/app/backend/venv/bin/uvicorn main:app \
    --host 127.0.0.1 \
    --port 8000 \
    --workers 2
Restart=always
RestartSec=5
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

> `--workers 2`：2 个 worker 进程对 60 人的并发完全够用，内存占用约 200 MB。

```bash
# 启用并启动服务
sudo systemctl daemon-reload
sudo systemctl enable aipm-backend
sudo systemctl start aipm-backend

# 确认运行状态
sudo systemctl status aipm-backend
```

### 3.4 构建前端

```bash
cd /opt/aipm/app/frontend

# 安装依赖
npm install

# 构建生产版本
npm run build
# 构建产物输出到 frontend/dist/
```

### 3.5 配置 Nginx

```bash
sudo nano /etc/nginx/sites-available/aipm
```

填入：

```nginx
server {
    listen 80;
    server_name _;          # 替换为实际 IP 或内网域名，如 192.168.1.100

    # 前端静态文件
    root /opt/aipm/app/frontend/dist;
    index index.html;

    # Vue Router history 模式支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 反向代理到后端 API
    location /api/ {
        proxy_pass         http://127.0.0.1:8000;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_read_timeout 120s;
        client_max_body_size 20M;
    }
}
```

```bash
# 启用站点，禁用默认站点
sudo ln -s /etc/nginx/sites-available/aipm /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default

# 检查配置语法
sudo nginx -t

# 重载 Nginx
sudo systemctl reload nginx
sudo systemctl enable nginx
```

### 3.6 验证部署

在同网段另一台电脑浏览器访问 `http://<服务器IP>`，能看到登录页则部署成功。

默认管理员账号：
- 用户名：`admin`
- 密码：`admin123`

**首次登录后请立即修改默认密码。**

---

## 4. 数据备份与恢复

### 4.1 需要备份什么

| 文件 | 位置 | 重要性 |
|------|------|--------|
| `app.db` | `backend/app.db` | ★★★ 所有业务数据 |
| `config.json` | `backend/config.json` | ★★☆ 配置参数 |
| 报表文件 | `issue_report_path` 指定目录 | ★★☆ 原始数据 |
| 问题单快照明细 | `issue_snapshot_dir`（默认 `backend/data/issue_snapshots/`） | ★★☆ 趋势数字在库里，明细钻取靠这些 JSON |
| 采集 Excel 备份 | `issue_excel_raw_dir` / `issue_excel_analysis_dir`（默认 `backend/data/issue_excel/`） | ★☆☆ 可再生成 |

### 4.2 SQLite 在线备份

SQLite 提供 `.backup` 命令，支持**数据库运行时**安全备份（不需要停服）：

```bash
sqlite3 /opt/aipm/app/backend/app.db ".backup /opt/aipm/backups/app-$(date +%Y%m%d-%H%M%S).db"
```

### 4.3 自动定时备份（推荐）

创建备份脚本：

```bash
sudo mkdir -p /opt/aipm/backups
sudo chown $USER:$USER /opt/aipm/backups

nano /opt/aipm/backup.sh
```

脚本内容：

```bash
#!/bin/bash
set -e

BACKUP_DIR="/opt/aipm/backups"
DB_PATH="/opt/aipm/app/backend/app.db"
CFG_PATH="/opt/aipm/app/backend/config.json"
DATE=$(date +%Y%m%d-%H%M%S)

# 备份数据库（在线热备份）
sqlite3 "$DB_PATH" ".backup ${BACKUP_DIR}/app-${DATE}.db"

# 备份配置文件
cp "$CFG_PATH" "${BACKUP_DIR}/config-${DATE}.json"

# 保留最近 30 份，删除更旧的
find "$BACKUP_DIR" -name "app-*.db"       -mtime +30 -delete
find "$BACKUP_DIR" -name "config-*.json"  -mtime +30 -delete

echo "[$(date)] 备份完成：app-${DATE}.db"
```

```bash
chmod +x /opt/aipm/backup.sh

# 测试一次
/opt/aipm/backup.sh
```

注册 cron 定时任务（每天凌晨 2:00 执行）：

```bash
crontab -e
```

添加：

```
0 2 * * * /opt/aipm/backup.sh >> /var/log/aipm-backup.log 2>&1
```

### 4.4 从备份恢复

**停服 → 替换数据库文件 → 重启**：

```bash
# 1. 停止后端服务
sudo systemctl stop aipm-backend

# 2. 替换数据库（先保留损坏的文件以备查）
cp /opt/aipm/app/backend/app.db /opt/aipm/app.db.broken
cp /opt/aipm/backups/app-20260517-020001.db /opt/aipm/app/backend/app.db

# 3. 重启服务
sudo systemctl start aipm-backend
```

### 4.5 建议：异地备份

将 `/opt/aipm/backups/` 目录定期同步到另一台机器或 NAS，防止服务器硬盘故障导致数据和备份同时丢失：

```bash
# 使用 rsync 同步到另一台机器（每天凌晨 3:00）
# 在 crontab 中追加：
0 3 * * * rsync -az /opt/aipm/backups/ backup-server:/backup/aipm/
```

---

## 5. 升级流程

### 5.1 升级前准备

每次升级前务必先备份：

```bash
/opt/aipm/backup.sh
```

### 5.2 标准升级步骤

```bash
cd /opt/aipm/app

# 1. 拉取最新代码
git pull origin main

# 2. 更新后端依赖（若 requirements.txt 有变化）
cd backend
source venv/bin/activate
pip install -r requirements.txt

# 2.5 数据库迁移：现已随后端启动自动完成（backend/automigrate.py）。
#     重启后端时会自动 stamp 基线（首次）并 upgrade 到 head，无需手动执行。
#     如需手动核验/排障，仍可运行：
#       alembic current      # 查看当前版本
#       alembic upgrade head # 手动补跑（正常情况下启动已做过，是空操作）

# 3. 重启后端（create_all 自动建新表；Alembic 迁移在启动时自动追平）
sudo systemctl restart aipm-backend
sudo systemctl status aipm-backend   # 确认启动正常

# 4. 重新构建前端
cd ../frontend
npm install           # 依赖有变化时自动处理
npm run build

# 5. 刷新 Nginx（静态文件已更新，无需重启，reload 即可）
sudo systemctl reload nginx
```

> 前端构建产物直接覆盖 `dist/`，Nginx reload 后用户刷新浏览器即可看到新版本。整个升级过程后端停服不超过 10 秒。

### 5.3 数据库结构变更

**v0.26.0 起改用 Alembic 管理结构变更。** 三层机制分工如下：

| 机制 | 负责 | 何时运行 |
|------|------|----------|
| `create_all`（`main.py` 启动时） | **新建表**（新增功能带来的整张表） | 重启后端自动 |
| Alembic（`backend/alembic/`） | **改名 / 删列 / 改类型 / 加约束 / 数据回填** | 重启后端自动（`automigrate.py`） |
| `migrate.py`（已冻结） | 历史遗留的 `ADD COLUMN`，不再新增 | 重启后端自动 |

✅ **自动迁移**：后端启动时 `automigrate.py` 会自动把 Alembic 追平到 head：
- 库还没被 Alembic 跟踪（无 `alembic_version` 表）→ 自动 `stamp 0001_baseline` 再 upgrade，
  首次从 v0.26 以前升级也**无需手动 stamp**；
- 已跟踪 → 直接 upgrade（没有新迁移时是毫秒级空操作）。
- 迁移失败只记日志、不阻塞启动；若功能异常，查 `journalctl -u aipm-backend` 里的
  `automigrate` 告警，手动 `alembic upgrade head` 排障。

> 升级前已用 `PRAGMA foreign_key_check` 核验生产库无悬空外键，启用外键强制是安全的。
> 若 `alembic upgrade` 报唯一约束/外键冲突，多半是存量脏数据，先按报错清理再重试。

若某次升级后出现 `500` 错误，优先查看后端日志：

```bash
sudo journalctl -u aipm-backend -n 100 --no-pager
```

### 5.4 特定版本升级说明

部分版本涉及数据格式或行为变更，升级时需额外注意。

---

#### v0.28.x → 问题单定时采集可配置/采集日志 + 客户面问题跟踪重构（含数据回填）

**变更内容**

1. **问题单管理：定时采集可配置 + 采集日志**
   - 每日自动采集**时刻可在页面配置**（问题单管理 → 配置 → 「定时采集」卡片：启用开关、执行时间 HH:MM、
     脚本超时秒数）。保存后调度**立即热更新，无需重启后端**。不再固定 07:30。
   - 新表 `issue_collect_logs`：每次采集（定时/手动）成败都留一条日志（耗时、条数、失败原因含脚本 stderr 尾部）。
     每个项目 tab 新增「采集日志」页签，采集失败时到这里查原因。
   - **手动「立即采集」改为异步**：后端起线程立即返回，前端轮询进度。修复了此前"点采集报失败、
     过几分钟刷新数据又出来了"的问题（根因是前端 axios 10 秒超时，而采集要跑几分钟）。
   - 采集脚本子进程超时从写死 120 秒改为可配置（`issue_script_timeout`，默认 600 秒）。
   - Tab 顺序调整：各项目 tab 前置（默认落在第一个项目），旧「本地报表」移到最后并改名「历史数据」。

2. **客户面问题/关键事务重构（本版核心数据变更）**
   - 原「客户面状态」总览里的 软件类风险和问题 / 现场关键事务 两栏，由 `customer_status` 表上两个
     Text 列里的 JSON 清单（`[{text,done}]`）**提升为独立实体表 `customer_issues`**（一行一条）。
     问题条目新增：关联问题单、紧急程度（重要紧急/紧急/一般）、责任人（系统用户+自由文本兜底）、
     提出/预计闭环/实际闭环时间、状态（OPEN/CLOSED/挂起）。
   - 条目类型共三种：软件类问题（issue）/ 关键事务（task）/ **需求（demand）**。在问题栏快速录入时
     以「需求:」开头记为需求、「问题:」开头或无前缀记为问题（前缀本身不入库）；需求与问题同栏展示，
     带蓝色「需求」角标，PPT 导出加 `[需求]` 前缀。
   - **「问题跟踪」作为「客户面状态」页的第二个 tab**（与机台总览同页，不单独占侧边栏；
     旧地址 `/customer-issues` 自动重定向）：全战场汇总表格，统计卡即筛选器、行内编辑、
     按状态着色（已闭环浅绿·与专项管理一致 / 挂起黄 / 逾期红）、Excel 导出。总览点条目可跳转聚焦。
   - 总览与客户详情页新增「显示已完成」开关，已闭环条目默认折叠成角标。
   - 每日 08:00 的临期/逾期扫描（`daily_ddl_scan`）纳入 `customer_issues`：未闭环且预计闭环时间
     临期 3 天内/已逾期的，通知责任人。
   - 客户面状态 PPT 导出改读新表（挂起条目标 ⏸）。

3. **遗留代码清理（随本版一并发布，无数据库变更）**
   - 下线两个只读遗留接口 `/api/versions`、`/api/iterations`（前端已无调用；首页版本计数改用大版本表）。
     旧 `versions` / `iterations` **表保留不动**，仅代码路径退役。
   - 停止 seed 这两张表的示例数据；删除若干死代码（`/api/issues/api-data` 等）。

**新增表**
- `issue_collect_logs`（`create_all` 启动自动建，无需迁移）
- `customer_issues`（`create_all` 也能建出**空表**，但存量数据回填必须跑 Alembic，见下）

**⚠️ 数据回填（必须跑 Alembic，但漏跑的表现与 0003 不同）**

迁移 `0004_customer_issues`：建表 + 把 `customer_status.key_issues` / `recent_focus` 的存量 JSON 清单
逐条搬进 `customer_issues`（`done=true → CLOSED`；闭环时间留空；两列**保留不删**，作回滚余地）。

- **漏跑不会 500**：`create_all` 会把表建出来（空表），页面能正常打开——但老的清单数据不显示，
  表现为"升级后关键事务/问题全没了"。补跑 `alembic upgrade head` 即恢复。
- **迁移是幂等的**：表已被 `create_all` 建过、或已回填过，重复执行都会自动跳过，不会造出重复数据。
  先重启后端再跑 alembic、或反过来，顺序无所谓。

**新增配置键**（`config.json`，页面「配置」tab 可改，均有默认值、可不手动配置）
- `issue_snapshot_enabled`（默认 true）/ `issue_snapshot_time`（默认 "07:30"）/ `issue_script_timeout`（默认 600）

**升级步骤**

```bash
cd /opt/aipm/app
/opt/aipm/backup.sh                       # 1. 先备份！
git pull origin main                      # 2. 拉代码（本版无新增 Python 依赖）

cd backend && source venv/bin/activate
alembic upgrade head                      # 3. ★必须★ 应用 0004（customer_issues 建表+回填）
alembic current                           # 确认版本 = 0004_customer_issues (head)

sudo systemctl restart aipm-backend       # 4. 重启后端
cd ../frontend && npm install && npm run build   # 5. 重建前端
sudo systemctl reload nginx
```

**验证**
- `alembic current` 显示 `0004_customer_issues (head)`；
- 打开「客户面状态」总览：关键事务/软件类问题两栏正常显示**升级前的老条目**（这是回填是否成功的直接标志）；
- 侧边栏出现「问题跟踪」，打开后统计卡有数、能按战场筛选；
- 问题单管理 → 配置 tab 出现「定时采集」卡片；改执行时间保存，提示"每日自动采集时间已设为 …"；
- 任一项目 tab → 「采集日志」页签能打开（首次为空正常）；
- 首页（项目简介）正常打开，版本计数显示大版本数量。

**回滚**
- 回退代码即可回到旧清单 UI：旧的两个 JSON 列还在库里未动，旧版前后端照常读写。
  ⚠️ 但**升级后在新表里新增/修改的条目不会同步回 JSON 列**——回滚会丢这部分增量（数据仍在
  `customer_issues` 表里，再升级回来即可见）。
- 彻底回滚数据库：`alembic downgrade -1`（只 DROP `customer_issues` 表，JSON 列数据不受影响）。
- `issue_collect_logs` 对旧代码无害，无需处理。

---

#### v0.27.x → 专项分段可排序/列格式 + 问题单 API 采集（手动触发 + 接入模板）

**变更内容**

1. **附加表格提升为独立分段**：专项/攻关详情页里，「专项事务」下方的附加表格（RichGrid）不再嵌在事务区块内部，
   而是**每张表各成一个与「专项事务」并列的独立分段**，标题即分段标题，右侧带「保存表格 / 删除整表」。
2. **表格支持按列设格式**：选中某列的正文单元格后，工具条「列格式」可把该列设为 **文本 / 下拉 / 日期**；
   选「下拉」时再填候选项（逗号分隔）。列格式存进 `extra_grids_json` 的 `colTypes` / `colOptions` 字段，
   随表持久化；插入 / 删除行列时自动对齐。旧表格无这两字段时按 `text` / `[]` 兜底。
3. **分段顺序可调（每个专项各自独立，编辑者可调）**：进入编辑模式后页面顶部出现「分段顺序」面板，
   可上下移动任意分段（含附加表格），例如把「求助」和「全景图」互换、把某张表格提到事务之前。
   顺序存进 `special_contents` 新增列 `section_order_json`（分段 key 的有序数组，空＝默认顺序）。
   附加表格分配了稳定 `gid` 作为分段 key（`grid:<gid>`），保存顺序时连同表格一起落库，跨刷新保持稳定。

**新增依赖**：`requests==2.32.3`（问题单 API 采集脚本 `fetch_issues_api.py` 用；`pip install -r requirements.txt` 自动装）。

**新增表**：`issue_snapshots` / `issue_snapshot_stats`（问题单采集，`create_all` 启动自动建，无需迁移）。

**⚠️ 新增列（本版本必须手动跑 Alembic，`migrate.py` 已冻结不再自动加列）**
- `special_contents`：`section_order_json`（`TEXT NOT NULL DEFAULT '[]'`，迁移 `0003_special_section_order` 会给存量行回填 `'[]'`）

**升级步骤（⚠️ 这是继 v0.26 后又一个"改了已有表结构"的版本，务必跑迁移）**

```bash
cd /opt/aipm/app
/opt/aipm/backup.sh                       # 1. 先备份！
git pull origin main                      # 2. 拉代码

cd backend && source venv/bin/activate
alembic upgrade head                      # 3. ★必须★ 应用 0003（给 special_contents 加列）
alembic current                           # 确认版本 = head

sudo systemctl restart aipm-backend       # 4. 重启后端
cd ../frontend && npm install && npm run build   # 5. 重建前端
sudo systemctl reload nginx
```

**⚠️ 风险点：漏跑迁移会直接 500**

与"只加新表"的版本不同，本版给**已有表加了列**。`create_all` 不会给已有表补列，`migrate.py` 也已冻结不含此列，
所以**如果忘了 `alembic upgrade head` 就重启后端，专项详情 / 内容相关接口会因 SELECT 到不存在的
`section_order_json` 列而报 500**。升级后先用 admin 打开任意专项详情页确认能正常加载。

**验证**
- `alembic current` 显示已到 head；
- 打开一个专项详情页 → 进入编辑 → 顶部出现「分段顺序」面板，上下移动能改变分段显示顺序并保存；
- 「+ 添加表格」新增的表格作为独立分段出现在底部，可上移；选中某列可切「下拉 / 日期」格式。

**回滚**
- 回滚到 v0.26.x：还原 app.db 即可；或保留数据库、只回退代码——旧代码的 `SpecialContent` 模型不映射
  `section_order_json`，SQLAlchemy 只 SELECT 已映射列，**多出来的这一列对旧代码无害**。
  附加表格的 `colTypes` / `colOptions` 也会被旧版 RichGrid 忽略（按纯文本渲染，不丢数据）。
- 如需彻底清列：`alembic downgrade -1`（会 `DROP COLUMN section_order_json`）。

**本版本还含：问题单 API 采集（手动触发 + 接入模板）**

- 问题单管理页每个项目 tab（YLS3000 等）新增管理员专用「立即采集」按钮：即时跑一次采集，明细写文件、
  聚合数字入库，采完自动刷新当前统计/趋势。日常仍由每天 **07:30** 定时任务自动采集。
- 采集脚本 `backend/scripts/fetch_issues_api.py` 重构为「填空式」：只需在 `fetch_from_api()` 里写你的 API
  调用、在 `FIELD_MAPPING` 左侧对齐字段名、把 `USE_SAMPLE` 改 `False`，其余标准化。未接入前
  `USE_SAMPLE=True` 返回示例数据，页面/链路可先跑通。
- 采集相关配置（问题单管理页「配置」tab 或直接改 `config.json`）：`issue_api_script_path`（脚本路径）、
  `issue_api_projects`（项目 tab 列表）、`issue_snapshot_dir`（明细文件目录，默认 `backend/data/issue_snapshots`）。
- 趋势只读库里聚合数字（`issue_snapshot_stats`），明细按需从 `issue_snapshot_dir` 的 JSON 文件加载。

> ⚠️ 采集脚本以 `python <脚本> <项目>` 由后端子进程调用，依赖后端 venv 里装好 `requests`，
> 所以升级务必 `pip install -r requirements.txt`。`issue_snapshot_dir` 需后端进程有写权限并纳入备份。

---

#### v0.26.x → 引入 Alembic + 启用外键约束 + 状态/优先级枚举统一

**变更内容**

1. **引入 Alembic** 作为数据库结构变更的系统（`backend/alembic/`，用法见 `backend/alembic/README.md`）。
   负责 `create_all` 做不到的改名 / 删列 / 改类型 / 加约束 / 数据回填；`migrate.py` 就此冻结，
   `create_all` 仍保留在启动路径建新表。**这是首个需要手动跑迁移的版本**（见下方升级步骤）。
2. **启用 SQLite 外键约束**：后端给每条连接加 `PRAGMA foreign_keys=ON`。此前 `ondelete=CASCADE/SET NULL`
   只是写进 DDL 不生效，现已在数据库层强制——删除被引用的主数据（如资源组）时，关联行的外键会按规则
   级联删除 / 置空，而不再留下指向已删行的"悬空外键"。
3. **状态/优先级枚举收口到 `backend/enums.py`**：进展状态 6 值（含「已变更」）、优先级 P0–P3 等统一管理。
   **两张需求表的优先级口径统一**：产品需求由「高/中/低」并入「P0–P3」，存量数据由迁移 `0002` 按
   高→P1 / 中→P2 / 低→P3 自动映射；Excel 导入遇旧值也自动转换，前端下拉同步只剩 P0–P3。

**新增依赖**：`alembic==1.13.2`。升级时 `pip install -r requirements.txt` 会自动装上。

**新增表**：无（不涉及新建业务表）。

**数据变更**：`iteration_product_requirements.priority` 历史「高/中/低」→「P1/P2/P3」（由 `0002` 迁移完成）。

**升级步骤（⚠️ 与以往不同，多了 Alembic 接入）**

```bash
cd /opt/aipm/app
/opt/aipm/backup.sh                       # 1. 先备份！
git pull origin main                      # 2. 拉代码

cd backend && source venv/bin/activate
pip install -r requirements.txt           # 3. 装 alembic

# 4. 一次性把现有库接入 Alembic（只在本次 v0.26 升级做一次）
alembic stamp 0001_baseline
alembic upgrade head                      # 应用 0002（优先级统一）
alembic current                           # 确认版本 = head

sudo systemctl restart aipm-backend       # 5. 重启（外键 pragma / 枚举校验生效）
cd ../frontend && npm install && npm run build
sudo systemctl reload nginx
```

> 之后的版本升级只需在标准流程里执行 `alembic upgrade head`（5.2 第 2.5 步），不必再 `stamp`。

**验证**

- `alembic current` 显示已到 head；
- 新建产品需求时优先级只剩 P0–P3；给需求传非法 priority/progress 返回 422；
- 删除一个被引用的资源组后，关联行的 `group_id` 在数据库里被置空（而非留悬空 id）。

**风险点**

- 这是**首个必须手动跑迁移**的版本，漏跑 `alembic upgrade head` 不会建表失败（无新表），
  但历史「高/中/低」的产品需求优先级不会被转换（生产库目前该表数据极少，影响有限）。
- 启用外键后，**删除主数据的行为更"严格"**：以往靠 ORM 兜底、裸 SQL 能删的悬空引用，现在会按
  `ondelete` 规则级联。升级前已核验生产库 `PRAGMA foreign_key_check` 为空，无存量违规。

**回滚**

- 回滚到 v0.25.x：还原 app.db 即可。`alembic_version` 表在旧代码下被忽略；旧代码不开外键 pragma，
  行为与升级前一致；产品需求若已转成 P 级，旧前端下拉是「高/中/低」会显示原值但选不中——重新选一次即可，
  数据不丢。

---

#### v0.10.x → 客户面状态清单字段格式变更

**变更内容**

`customer_status` 表的两个文本字段存储格式由纯文本改为 JSON 数组：

| 字段 | 旧格式（纯文本） | 新格式（JSON） |
|------|-----------------|----------------|
| `recent_focus` | `"良率提升\n备件采购"` | `[{"text":"良率提升","done":false},{"text":"备件采购","done":false}]` |
| `key_issues` | `"主控板偶发复位"` | `[{"text":"主控板偶发复位","done":false}]` |

**是否需要修改数据库？不需要。**

两个字段仍然是 `TEXT` 类型，表结构无任何变化。旧数据可直接使用，无需运行任何迁移脚本。

**兼容原理**

前端在读取字段时会自动判断格式：

```
读取字段值
    ├── 能解析为 JSON 数组 → 直接作为清单条目渲染（新格式）
    └── 否则按换行符拆分  → 每行作为一个未勾选条目渲染（旧格式自动兼容）
```

旧数据在用户第一次对该行执行操作（勾选、新增条目、点「保存」）后，才会被写成新 JSON 格式，之前一直保持原样。

**PPT 导出兼容**

`pptx_utils.py` 已同步更新，导出时自动将 JSON 清单转为可读文本：

```
✓ 已完成的条目
· 未完成的条目
```

旧纯文本字段原样输出，不受影响。

**升级操作**

无额外步骤，按 5.2 标准流程执行即可：

```bash
git pull origin main
sudo systemctl restart aipm-backend
cd frontend && npm install && npm run build
sudo systemctl reload nginx
```

---

#### v0.12.x → 新增项目一本通 + 专项管理

**变更内容**

1. 新增页面「项目一本通」：管理员维护自定义分类与条目（外链/文件上传），普通用户只读、可下载。
2. 新增页面「专项管理」：管理员配置专项列表，每个专项一个详情页（专项目标 / 里程碑计划 / 一句话进展 / 全景图 / 事务表 / 风险问题表 / 阵型）。
3. 左侧导航的"专项管理"为二级菜单，展开后可见每个启用专项；管理员额外多一项"专项配置"用于增删改专项。
4. 新建数据库表：`handbook_categories` / `handbook_items` / `specials` / `special_contents` / `special_tasks` / `special_risks`。

**是否需要修改数据库？不需要手动操作。** SQLAlchemy `create_all` 会在后端启动时自动建表。

**新增文件上传目录**

- 一本通文件：`backend/uploads/handbook/<yyyymm>/<uuid>.<ext>`
- 专项全景图：`backend/uploads/specials/<special_id>/<uuid>.<ext>`

升级时确保后端进程对 `backend/uploads/` 目录有写权限：

```bash
mkdir -p /opt/aipm/app/backend/uploads
chown -R aipm:aipm /opt/aipm/app/backend/uploads
```

**备份建议**

`backend/uploads/` 是用户上传的文件，须与 `app.db` 一起备份：

```bash
# 在 5.3 的备份脚本中追加
tar -czf "$BACKUP_DIR/uploads-$(date +%F).tar.gz" -C /opt/aipm/app/backend uploads
```

**升级操作**

无额外步骤，按 5.2 标准流程执行即可。升级后用 admin 登录，先去「专项管理 → 专项配置」新增一个专项试一下。

---

#### v0.24.x → 专项富表格列宽拖动 + 多人编辑锁

**变更内容**
- 「专项事务」下方的附加表格（RichGrid）支持**拖动表头右边缘调整列宽**；列宽随表持久化在 `extra_grids_json` 的 `colWidths` 字段，保存 / 刷新后保留，插入 / 删除行列时自动对齐。旧表格无 `colWidths` 时按默认列宽补齐。
- **多人并发编辑锁**：专项详情页同一时刻只允许一人处于编辑模式。
  - 进入编辑时向服务端取锁（`POST /api/specials/{id}/lock`），编辑期间每 60s 心跳续期，「完成编辑」或离开页面时释放。
  - 他人持锁时，本页顶部显示**只读横幅**（「XX 正在编辑…」），普通用户「进入编辑」按钮置灰；**管理员**可「强制接管」（接管后原编辑者再保存会被拒）。
  - 锁 **TTL 180s**：持锁人异常关闭页签（心跳停止）后，最长 3 分钟自动失效，他人即可接管，**不会出现永久死锁**。
  - 服务端在内容 / 全景图 / 事务 / 风险的所有写接口加了**前置校验**：他人持新鲜锁时返回 **423**，前端弹「正被他人编辑，暂时无法保存」。无人持锁时照常放行（向后兼容）。
- **修复内容串台**：`<router-view>` 改为按 `route.fullPath` 作 key，专项间切换（`/specials/1→/specials/2`）会重建组件实例，杜绝迟到异步响应把上一个专项的数据写到当前页；`load()` 另加请求令牌兜底。
- **Excel 导出优化**：
  - 「专项计划里程碑」由表格改为**时间轴图片**（PIL 绘制后嵌入，节点按状态着色 + 名称/日期 + 图例），不再是表格。
  - 「专项事务」下方的**附加表格现在会导出**：每个表 → 一张**独立工作表**（用各自 `colWidths` 定列宽，保留表头合并 / 对齐 / 字体颜色，**表头加粗**）。
- 附加表格在**页面上**的表头也强制加粗显示（原 600 → 700）。

**新增依赖**：`pillow>=9.0.0`（绘制里程碑图片；此前作为 python-pptx 的间接依赖已存在，现显式声明）。升级时 `pip install -r requirements.txt` 会自动装上。

**⚠️ Linux 服务器中文字体**：里程碑图片用 PIL 绘制中文，需系统装有中文字体，否则中文显示为方块。Ubuntu 安装：
```bash
sudo apt-get install -y fonts-noto-cjk    # 或 fonts-wqy-microhei
```
代码已自动探测 Windows / Noto / 文泉驿 / 思源等常见字体路径（含常规目录扫描），并会**校验字体真的带汉字字形**；
都找不到时里程碑会**自动退回表格形式**（不影响其它内容导出，绝不再输出方块乱码）。
字体装在非常规目录时，可用环境变量 `APP_CJK_FONT=/path/to/font.ttf` 直接指定。

**新增表（create_all 自动建）**
- `special_edit_locks`（每专项至多一行：持锁人 / 取锁时间 / 最后心跳）

**新增列**：无（`colWidths` 存在既有的 `extra_grids_json` 文本里，无需 schema 变更）。

**多 worker 注意**：编辑锁是**数据库表**实现，跨进程 / 跨 worker 一致，Uvicorn 开多 worker 也有效。

**升级步骤**：按 5.2 标准流程即可（记得 `pip install -r requirements.txt` 装 Pillow + 服务器装中文字体），首次启动 `create_all` 自动建表。

**回滚**：回滚到 v0.23.x 还原 app.db 即可；`special_edit_locks` 表在旧代码下被忽略，`colWidths` 字段被旧 RichGrid 忽略（按默认列宽渲染，不丢数据）。

---

#### v0.23.x → 专项详情页优化

**变更内容**
- 「一句话进展 & 求助」拆分为**整体进展** + **求助**两个独立区块（求助存 `special_contents.help_request`）。
- 「风险和问题」区块调整到「专项事务」之**前**；周报（.eml 草稿）与 Excel 导出同步调整章节顺序与编号。
- 专项**全景图上传权限放开**：由「仅管理员」改为任意登录用户（后端早已允许，本次同步前端按钮显隐）。
- 富文本编辑器（目标 / 进展 / 求助 / 事务 / 风险）新增**字体族选择**（微软雅黑 / 宋体）与**红 / 蓝 / 黑快捷颜色**；专项页默认字体微软雅黑。
- 「专项事务」下方「+ 添加表格」改用新的 **RichGrid** 组件：支持**合并 / 拆分表头**、单元格**居中 / 左对齐**、正文**字体颜色（黑 / 红 / 蓝）**、**删除行 / 删除列组**。
- **修复**：附加表格在切换专项时会显示上一个专项数据的串台 bug（FormationGrid / RichGrid 改为响应式读取，并用稳定 key）。
- 界面细节：分段标题统一 18px、子项内容默认 16px；风险/事务表格正文文字由灰改为近黑，**已闭环行**单独浅绿底色，并提供「显示已闭环」开关随时收起闭环项。
- **编辑模式**：标题栏新增「进入编辑 / 完成编辑」按钮，仅在编辑模式下显示各类增删改控件；点「完成编辑」会自动落库附加表格与阵型的未保存改动后退出。
- RichGrid 增强：支持**在指定位置插入行（上 / 下）/ 列（左 / 右）**，并补齐「删除行 / 删除列」按钮。
- Excel 导出重写为统一 6 列网格（标题/章节/叙述横跨合并、窄表按列合并对齐、按内容估算行高自动换行），解决"导出很乱无法直接使用"。

**新增依赖**：无。

**新增列（migrate.py 自动 ALTER）**
- `special_contents`：`help_request`

**升级步骤**：按 5.2 标准流程即可。`extra_grids_json` 兼容旧格式（旧的纯字符串表格会自动升级为可着色 / 可合并的富表格）。

**回滚**：回滚到 v0.22.x 还原 app.db 即可；新列在旧代码下被忽略，富表格 JSON 旧代码按纯文本网格读取（颜色 / 合并信息忽略，不丢数据）。

---

#### v0.22.x → 客户详情：机台里程碑 + 自定义信息块

**变更内容**
- 客户详情页每台机台「当前进展」上方新增**里程碑图**：固定 6 节点（出厂 → Tier0 → Tier1 → Tier2 → Tier3 → 验收），可设状态+日期，存 `customer_status.milestones_json`，所有登录用户可编辑。
- 「SOW 状态」下方新增**自定义信息块**：管理员在「客户管理 → 信息块配置」全局定义（如 MPH状态），每台机台渲染为「一段文本 + 一个附件/图片」。信息块的文本/附件由管理员维护。
- 「问题单情况」上方新增**客户定制化需求**表（客户级）：序号 / 需求描述 / 对客户价值 / 领域 / 设计人员 / 是否涉及其他项目 / 预计合入版本 / 备注，admin 增删改。

**新增依赖**：无。

**新增表（create_all 自动建）**
- `customer_extra_fields`（信息块定义，全局共享）
- `customer_extra_values`（每台机台每个块的文本+附件）
- `customer_custom_reqs`（客户定制化需求，客户级）

**新增列（migrate.py 自动 ALTER）**
- `customer_status`：`milestones_json`

**升级步骤**：按 5.2 标准流程即可。附件落在 `backend/uploads/customer_extra/<machine_status_id>/`，记得纳入备份范围。

**风险点**：新增列可空、新表对既有数据零破坏；删除信息块会连带删除所有机台下该块的文本与附件，建议改用"停用"。

**回滚**：回滚到 v0.21.x：还原 app.db 即可，新表/新列在旧代码下被忽略。

---

#### v0.21.x → 自动阵型图 + 工时名单 + 版本质量统计

**变更内容**

- 干系人管理「项目阵型 → 阵型图」新增「组织阵型图（自动）」模式：按 部门 → PL 组 → 成员 自动渲染组织树，随
  组织架构与用户挂靠实时更新；原「上传图片」模式保留，通过单选切换。
- 「人员名单」tab 更名为「工时名单」（功能不变，仍可编辑 + Excel 导入导出）。
- 用户创建/编辑对话框已可直接选择「所属 PL 组」（部门自动反推），新建后即可作为 owner 被选用（v0.20 已具备，本次延续）。
- 领域需求表（`iteration_requirements`）新增「版本质量统计」：合入链接（支持多个，每行一个）、代码量、自验证用例数、转测后问题单数量。
- 「度量看板 → 版本完成率」汇总展示该版本的总代码量 / 自验证用例数 / 转测后问题单数。
- 「度量看板 → 迭代质量」新增按年度逐迭代的质量表：代码量、自验证用例数、自验证用例密度(个/kloc)、转测后问题单数、转测后问题单密度(个/kloc)。
- **通知接全**：客户面状态、专项/攻关（含事务/风险）写入时会给订阅者发通知；专项详情页加「订阅」按钮。
- 顶部新增**通知跑马灯**：登录后在页头下方横向滚动播放未读通知，点击跳转、可关闭（关闭状态存 sessionStorage，仅当前会话）。

**新增依赖**：无。

**新增表**：无。

**新增列（migrate.py 自动 ALTER）**
- `iteration_requirements`：`merge_links`、`code_volume`、`self_test_case_count`、`post_test_issue_count`

**升级步骤**：按 5.2 标准流程即可，无额外手工操作。自动阵型图依赖 v0.20 的「组织架构 + 用户管理」数据，
若组织架构未维护，阵型图自动模式会提示先去建部门 / PL 组。

**风险点**：新增列均可空（`merge_links` 默认空串，三个计数列可空），对既有数据零破坏。

**回滚**：回滚到 v0.20.x：还原 app.db 即可，新列在旧代码下被忽略，无需额外处理。

---

#### v0.20.x → 数据主线 + 通知系统 + 度量看板（重大重构）

**变更内容**

本次升级横跨 4 个子里程碑，对数据模型、权限、通知三大基础设施做了较大重构：

**M1：主数据骨架**
- 新增 `resource_groups` 表（部门 + PL 组两级结构）。
- `users` 表加列：`emp_no`、`group_id`、`job_title`、`can_login`。
- 新增「组织架构」管理页（admin），位于左侧菜单；用户管理页升级，可配置部门 / PL 组归属。
- 登录拦截：`can_login=false` 的账号不能登录（用于"纯人员档案"）。

**M2：客户与人员 FK 化**
- `customer_status`、`stakeholder_battlefields` 加 `customer_id` FK；`project_formation_members` 加 `user_id` FK。
- 新增「数据对账」页（admin），支持：
  - 自动按 `code/别名` 回填客户 FK；
  - 自动按 `工号/姓名` 回填人员 FK；
  - 剩余未匹配项手动认领或一键建档（建出 `can_login=false` 的 User）。
- 写入时自动反查 FK（新数据无需手工对账）。
- 客户详情页"机台"列表优先走 FK，向后兼容字符串匹配。

**M3：业务 owner / version FK + 度量看板**
- 多张业务表加 owner_user_id / group_id / target_version_id FK：
  `iteration_requirements`、`iteration_product_requirements`（feature_fo/se/tfo 各一个）、
  `annual_iterations`、`specials`、`special_tasks`、`special_risks`、`handbook_items`。
- 字符串 `owner` / `owner_group` / `planned_version` 字段保留作展示快照；写入时自动反查 FK。
- 新增 `/api/metrics/version/{id}`、`/api/metrics/iteration/{id}`、`/api/metrics/group/{id}` 三个度量端点。
- 新增「度量看板」页（所有用户可见）：版本完成率 / 迭代质量 / 组级负载三个 tab。

**M4：站内通知系统**
- 新增 `notifications`、`notification_reads`（广播已读）、`subscriptions` 三张表。
- 头部加铃铛（带未读小红点，30s 轮询未读数）+ 抽屉式通知中心。
- 客户详情页加「订阅」按钮（其他详情页可按需复用 `SubscribeButton.vue`）。
- admin 在通知抽屉可发"广播"通知给所有人。
- 后台 `APScheduler` 每天 08:00（Asia/Shanghai）扫描 `special_tasks` / `special_risks`，对未关闭、
  `planned_close_date` 落在未来 3 天内或已逾期的，给 owner 发通知。
- 当前已接入 dispatch 的写入点：
  - `iteration_requirements` 更新时（owner 变更 → assignment / 进展变更 → status_change）。
  - 其余表（customer_status、specials 等）可在后续按需补接 `notify.dispatch(...)` 调用。

**新增依赖**
- `apscheduler==3.10.4`：升级时必须 `pip install -r requirements.txt`。

**新增表（create_all 自动建）**
- `resource_groups`、`notifications`、`notification_reads`、`subscriptions`。

**新增列（migrate.py 自动 ALTER）**
- `users`：`emp_no`、`can_login`、`group_id`、`job_title`
- `customer_status`、`stakeholder_battlefields`：`customer_id`
- `project_formation_members`：`user_id`
- `annual_iterations`：`owner_user_id`
- `iteration_requirements`：`owner_user_id`、`group_id`、`target_version_id`
- `iteration_product_requirements`：`target_version_id`、`feature_fo_user_id`、`feature_se_user_id`、`feature_tfo_user_id`
- `specials`、`special_tasks`、`special_risks`、`handbook_items`：`owner_user_id`

**升级步骤**

按 5.2 标准流程；额外注意：
1. 升级后用 admin 登录，先去「组织架构」建好部门 / PL 组。
2. 去「用户管理」给已注册用户补上工号、PL 组归属。
3. 去「数据对账」分别跑「自动回填」按钮 —— 客户对账走 code/别名；人员对账走工号/姓名。
4. 剩余未匹配项在"数据对账"页手动认领；阵型成员可以一键建档。

**风险点**
- `migrate.py` 失败时检查 `users` 表是否已有 `can_login` 列：旧库可能因 booleans 旧 SQLite 不支持 default 1 报错。已用 `BOOLEAN NOT NULL DEFAULT 1`，SQLite 兼容。
- `APScheduler` 启动失败被 main.py 吞掉，不会阻塞 API；但通知中的"临期提醒"将失效，请检查日志。
- 业务表 owner 字符串没有人对账时，FK 为空 → 度量看板的"组级负载"会少算；建议升级后第一周内尽快做对账。

**回滚**

回滚到 v0.17.x：备份 app.db 还原。新表（resource_groups / notifications / notification_reads /
subscriptions）和新列在 v0.17.x 代码下会被忽略，不会引发错误，但相关 UI 入口需把以下路由从前端打包里移除：
`/resource-groups`、`/data-mapping`、`/metrics`。

---

#### v0.17.x → 客户详情页新增机台 tabs + SOW + License + 问题单视图

**变更内容**

1. **客户详情页结构调整**（路径不变，仍是 `/customers/:id`，全屏页面）：
   - 区块 1：基本信息（把"别名"合并进基本信息卡，不再独立成块）。
   - 区块 2：**客户面机台 tabs**（横向切换）。机台来自「客户面状态」总览中 `battlefield ∈ {customer.code, ...aliases}` 的所有行；每台机台一个 tab，tab 名 = `machine_id`。
   - 每个机台 tab 内含三个子区：
     - **当前进展**：编辑该机台的 `customer_status` 字段，与「客户面状态」总览**完全同步**（同一行、同一字段）。
     - **SOW 状态**：表格，**列由 admin 在「客户管理 → SOW 字段配置」全局维护**；所有客户共用同一套列定义。每一行的字段值按列定义动态渲染（文本/日期/下拉）。
     - **License 管理**：每台机台支持上传多个 license 文件 + 备注；普通用户可下载，admin 可上传/删除。
   - 区块 3：**问题单情况**（在 tabs 外，整客户范围）。从最新一期问题单报表中按客户编码/别名过滤展示，给出按严重程度的小计与明细。
2. 「客户面状态」页面的「返回」按钮指向**总览**而非「客户管理」（客户管理仅 admin 可见，客户详情所有用户都可看）。

**是否需要修改数据库？需要，但全部自动完成。**

- 新建三张表，由 SQLAlchemy `create_all` 在后端启动时创建：
  - `sow_field_defs`：全局 SOW 列定义（key / label / field_type / options / sort_order / is_active）。
  - `sow_rows`：每台机台下的 SOW 行；FK `machine_status_id → customer_status.id`，ON DELETE CASCADE；`data` 字段是 JSON Text 存 `{key: value}`。
  - `machine_licenses`：每台机台的 license 文件登记；FK `machine_status_id → customer_status.id`，ON DELETE CASCADE。
- **`customer_status` 表完全未动**；老数据照旧。
- 新上传目录：`backend/uploads/licenses/<machine_status_id>/<uuid>.<ext>`，首次上传时自动创建，权限沿用 `uploads/` 现有归属。

**对老数据的兼容**

- 新表不依赖任何老数据，可独立存在。
- 问题单视图复用现有 `/api/issues/data` 接口，按 `category`（标题分类）字段做 `customer.code` + 所有别名的大小写不敏感包含匹配；老数据中 `category` 与客户名一致的会被自动归并到对应客户。
- SOW 行通过 `machine_status_id` 与机台行强绑定，**如果在总览中删除该机台行，对应的 SOW 行和 license 文件登记会随级联删除一起消失**（license 物理文件随之孤立）；偶发误删可通过备份恢复，日常使用风险低。

**升级操作**

无额外步骤，按 5.2 标准流程执行：

```bash
git pull origin main
sudo systemctl restart aipm-backend     # 自动建 3 张表
cd frontend && npm install && npm run build
sudo systemctl reload nginx
```

**升级后建议**

1. admin 登录 → 「客户面状态 → 客户管理」→ 点击右上「SOW 字段配置」预先把 SOW 表头列建好（推荐：事项名称 / 计划完成 / 实际完成 / 状态 / 备注）。
2. 确认 `customer_status` 中各行的 `battlefield` 已经对应到正确的客户 code 或别名，否则机台 tabs 不会出现。
3. 确认 `issue_report_path` 已正确配置；问题单视图依赖最新一期报表的 `category` 字段。

**回滚说明**

如需回滚到 v0.16.x：

- 代码回退即可，数据库**无需回退**：旧代码不查 `sow_*` / `machine_licenses` 三张表，对正常业务无任何影响。
- 想彻底清理：停服后 SQLite 里执行
  ```sql
  DROP TABLE machine_licenses;
  DROP TABLE sow_rows;
  DROP TABLE sow_field_defs;
  ```
  以及删除 `backend/uploads/licenses/` 目录。

---

#### v0.16.x → 新增客户主数据 + 干系人战场矩阵改为客户下拉

**变更内容**

1. 新增"客户管理"页面（仅 admin），路径 `/customers`，作为客户身份的单一来源。每个客户有：
   - 业务主键 `code`（缩写英文名，全局唯一）
   - 完整名称、区域、行业、备注、状态等基础字段
   - 多个**别名**（全局唯一），用于把老的散字符串映射到同一客户
2. 左侧导航的"客户面状态"改为二级菜单：**总览**（原客户面状态表格） + **客户管理**（admin 可见）。
3. 客户面状态表格里点击「客户」列 → 跳转该客户的完整详情页 `/customers/:id`（基本信息 + 别名编辑）。
4. **干系人管理 · 战场沟通矩阵**：
   - 表头第一列 "战场" → "客户"。
   - 新增/编辑弹窗的"客户"字段从自由输入框改为下拉，**选项来自客户管理**。
   - 后端 `stakeholder_battlefields.battlefield` 列**未改名、未改类型**，仍是字符串；只是 UI 行为收紧到从下拉选 `customer.code`。

**是否需要修改数据库？需要，但全部自动完成。**

- 新建两张表：`customers`、`customer_aliases`，由 SQLAlchemy `create_all` 在后端启动时创建。
- 新增三列：`customers.industry`、`customers.intro`、`customers.key_focus`，由 [`migrate.py`](../backend/migrate.py) 在启动时 `ALTER TABLE ADD COLUMN` 自动补齐（覆盖中间状态下表已被建过的情况）。
- 现有 `customer_status`、`stakeholder_battlefields` 等所有表的字段**完全未动**，老数据照旧。

**对老数据的兼容**

- `customer_status.battlefield`、`stakeholder_battlefields.battlefield` 里那些历史字符串（如 "华东客户A"）**继续可读、可显示**，不会被自动转换或清理。
- 编辑老的战场矩阵条目时，下拉里如果选项与历史字符串对不上，select 会显示原始值但选不中；想消化老数据时，admin 去客户管理把这些历史叫法**加为对应客户的别名**就行（别名全局唯一，加进去后即关联）。
- 新增的机台 / 战场条目从此走客户下拉，保证后续数据归一。

**升级操作**

无额外步骤，按 5.2 标准流程执行：

```bash
git pull origin main
sudo systemctl restart aipm-backend     # 自动建表 + 自动 ALTER 加列
cd frontend && npm install && npm run build
sudo systemctl reload nginx
```

**升级后建议**

1. admin 登录 → 「客户面状态 → 客户管理」→ 把现网在用的客户都建一遍，code 用稳定的英文缩写。
2. 把每个客户在历史 `battlefield` 字符串里出现过的所有写法，**作为别名挂在该客户名下**（chip 形式增减），后续按客户聚合时就能命中老数据。
3. 干系人 · 战场沟通矩阵的老条目，建议逐个打开编辑、从下拉重新选一次客户，让 `battlefield` 字段落到统一的 `code` 上。

**回滚说明**

如需回滚到 v0.15.x：

- 代码回退即可，数据库**无需回退**：`customers`、`customer_aliases` 两张新表对旧代码无影响（旧代码不查它们）；新加的 3 列对老 SELECT 也无影响。
- 如果想彻底清理：停服后 SQLite 里 `DROP TABLE customer_aliases; DROP TABLE customers;` 再启动，业务无任何依赖。

---

#### v0.11.x → 新增操作日志表 + 闲置自动登出

**变更内容**

1. 新增 `operation_logs` 表，记录登录与关键写操作（新增/修改/删除/导入/导出 PPT/运行脚本/修改配置 等），管理员可在前端「操作日志」页面分页查询、过滤。
2. 前端新增闲置自动登出：默认 **15 分钟** 无鼠标/键盘/滚动事件即清空会话并跳转登录页；多 tab 通过 `localStorage` 共享活动时间，任一 tab 在使用其它 tab 都不会被判定为闲置。
3. 任一 tab 主动登出（点「退出登录」、修改密码、收到 401）会通过 `localStorage` 事件广播给其它 tab 同步退出。

**是否需要修改数据库？不需要手动操作。**

`operation_logs` 表会在后端重启时由 SQLAlchemy 的 `create_all` 自动创建，与现有数据无冲突。

**注意事项**

- 操作日志只增不删，若长期运行体量较大，可在 SQLite 中按时间删除老数据：
  ```sql
  -- 例如保留最近 6 个月
  DELETE FROM operation_logs WHERE created_at < datetime('now','-6 months');
  VACUUM;
  ```
- 闲置阈值若需调整，修改 [frontend/src/App.vue](../frontend/src/App.vue) 中的常量 `IDLE_MS`。
- 前端会在「退出登录」时主动调用 `POST /api/auth/logout`，仅用于写一条登出日志，后端无服务端会话状态。

**升级操作**

无额外步骤，按 5.2 标准流程执行即可。

---

### 5.5 回滚

如果升级后发现严重问题，可快速回滚：

```bash
cd /opt/aipm/app

# 回退代码
git log --oneline -10         # 找到上一个版本的 commit hash
git checkout <commit-hash>

# 重启后端
sudo systemctl restart aipm-backend

# 重建前端
cd frontend && npm run build
sudo systemctl reload nginx
```

---

## 6. 常见问题排查

### 后端无法启动

```bash
sudo journalctl -u aipm-backend -n 50
```

常见原因：
- Python 版本不符：确认 `venv/bin/python --version` 为 3.11+
- 端口占用：`sudo ss -tlnp | grep 8000`
- `config.json` 格式错误：用 `python3 -m json.tool config.json` 验证

### 前端页面空白 / 404

```bash
sudo nginx -t                          # 检查 Nginx 配置语法
ls /opt/aipm/app/frontend/dist/        # 确认构建产物存在
sudo tail -f /var/log/nginx/error.log  # 查看 Nginx 错误日志
```

### API 请求 502 Bad Gateway

后端服务未运行或崩溃：

```bash
sudo systemctl status aipm-backend
sudo systemctl restart aipm-backend
```

### 忘记 admin 密码

用救急脚本重置（v0.28 起自带；顺带解除停用/禁止登录状态）：

```bash
cd /opt/aipm/app/backend
source venv/bin/activate
python scripts/reset_password.py admin 新密码
# 不带参数 = 把 admin 重置为 admin123；也可重置任意本地账户：
# python scripts/reset_password.py zhangsan 新密码
```

脚本只依赖 bcrypt + 标准库 sqlite3，**后端运行中也可安全执行**（WAL 模式），改完下次登录即生效。
登录后请立即在「用户管理」中改成正式密码。

### 查看实时日志

```bash
# 后端日志（实时）
sudo journalctl -u aipm-backend -f

# Nginx 访问日志
sudo tail -f /var/log/nginx/access.log
```

---

## 附录：服务管理速查

| 操作 | 命令 |
|------|------|
| 启动后端 | `sudo systemctl start aipm-backend` |
| 停止后端 | `sudo systemctl stop aipm-backend` |
| 重启后端 | `sudo systemctl restart aipm-backend` |
| 查看后端状态 | `sudo systemctl status aipm-backend` |
| 重载 Nginx | `sudo systemctl reload nginx` |
| 手动备份 | `/opt/aipm/backup.sh` |
| 查看备份列表 | `ls -lh /opt/aipm/backups/` |
| 查看后端日志 | `sudo journalctl -u aipm-backend -n 100` |
